# 贡献指南

感谢你考虑为本仓库贡献内容！本仓库是 Kimi K3 的技术资料整理与工程实践汇编，最欢迎的贡献是**实测数据**与**纠错**。

## 贡献类型与要求

### 1. 性能数据（最受欢迎 ⭐）

性能数据分为“外部实测”和“本项目复现”两层。外部公开数据登记到《自部署实验手册》§4.1 的索引；只有按仓库方法实际执行的结果，才能填入 §5 的本项目结果表。**没有环境上下文的数字不会被合并**——每条数据必须附带：

- [ ] 数据类型与复现状态（本人 / 本项目实测、外部公开实测；是否由本项目复现）
- [ ] 原始来源、测试日期与模型版本 / 权重哈希
- [ ] GPU 型号与数量（如 8× H100 80GB）
- [ ] 推理引擎与**确切版本号**（vLLM / SGLang / TokenSpeed，附 release 链接更佳）
- [ ] 完整启动参数（TP / EP / PP、max-model-len、prefix caching 开关等）
- [ ] 压测脚本参数（建议直接用 `examples/k3_selfhost_bench/bench_serving.py`，并附上输出的 JSON）
- [ ] 输入 / 输出长度、并发数、请求数、预热方法、错误率与指标定义

提交外部公开实测时，还需满足：

- 测试发生在公开权重发布之后，且原始页面明确说明是真机测试而非估算；
- 提供原始结果链接；若来源许可不明确，只提交事实摘要与链接，不复制或上传其原始文件；
- 明确标注“外部实测，项目未复现”，不得直接写入本项目结果表。

建议使用 issue 模板 **"实测数据提交"** 先开 issue 讨论，再提 PR。

### 2. 纠错（数据 / 表述 / 链接）

- 引用错误：请给出**一手来源链接**（官方博客、模型卡、论文、源码文件）和原文摘录；
- 本仓库的数据优先级：**官方模型卡 / 技术报告 > 官方博客 / API 文档 > 论文 > 第三方评测 > 第三方报道**。低优先级来源与官方冲突时以官方为准；
- 标明是"官方口径"还是"第三方实测"——两者同样有价值，但不能混淆。

### 3. 代码（examples/）

- 新示例需可运行：`pip install -r requirements.txt` 后一条命令能跑；
- 提交前至少通过 `python -m py_compile`；
- 代码中**不得**包含任何 API key 或真实业务数据；
- 在对应目录的 README 中写明用途与运行方式。

### 4. 专题文章（posts/）

- 工程向、可复现优先；纯观点 / 营销向内容不适合本仓库；
- 所有关键数字必须标注来源与日期；实测结论必须附带环境上下文；
- 明确区分三类内容：官方结论、第三方结论、你的实测结论。

## 流程

1. Fork 或新建分支（命名建议：`fix/...`、`feat/...`、`data/...`）
2. 提交 PR，标题格式：
   - `docs: ...`（文档修订）
   - `feat: ...`（新文章 / 新代码）
   - `data: ...`（实测数据补充）
   - `fix: ...`（纠错）
3. PR 描述中说明：改了什么、依据的一手来源、（如适用）实测环境上下文

## 行为准则

- 就事论事，尊重其他贡献者；
- 不发布未经证实的"内部消息"或来源不明的跑分截图；
- 本仓库内容为 CC BY-NC 4.0，引用的原始资料版权归原权利方所有——转载官方 / 第三方内容时注意引用比例，以讲解为目的的合理引用为宜。
